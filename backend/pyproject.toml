[project]
name = "protein-ai-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "biopython",
    "fastapi",
    "numpy",
    "py3Dmol",
    "pydantic",
    "requests",
    "scikit-learn",
    "scipy",
    "uvicorn",
]
